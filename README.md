# Schedule-node

## Setting

```shell
yarn init
yarn add amqplib node-schedule
yarn add dotenv
yarn add -D eslint eslint-config-prettier eslint-plugin-prettier prettier
node_modules/.bin/eslint --init
```
